const header=document.querySelector('#siteHeader'),navToggle=document.querySelector('.nav-toggle'),navMenu=document.querySelector('#navMenu'),backTop=document.querySelector('.back-top');
const setHeader=()=>header.classList.toggle('scrolled',scrollY>28);setHeader();addEventListener('scroll',()=>{setHeader();backTop.classList.toggle('show',scrollY>600)},{passive:true});
navToggle.addEventListener('click',()=>{const open=navMenu.classList.toggle('active');header.classList.toggle('open',open);navToggle.setAttribute('aria-expanded',open)});
navMenu.addEventListener('click',e=>{if(e.target.matches('a')){navMenu.classList.remove('active');header.classList.remove('open');navToggle.setAttribute('aria-expanded','false')}});
backTop.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
const revealObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');revealObserver.unobserve(entry.target)}})},{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>revealObserver.observe(el));
const countObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;const el=entry.target,target=Number(el.dataset.count);let duration=1400,then=performance.now();const tick=now=>{const p=Math.min((now-then)/duration,1);el.textContent=Math.floor(p*target).toLocaleString();if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick);countObserver.unobserve(el)})},{threshold:.7});document.querySelectorAll('[data-count]').forEach(el=>countObserver.observe(el));
const form=document.querySelector('.contact-form'),status=document.querySelector('.form-status');form.addEventListener('submit',e=>{e.preventDefault();let ok=true;form.querySelectorAll('input,textarea').forEach(field=>{const valid=field.checkValidity();field.classList.toggle('invalid',!valid);if(!valid)ok=false});status.textContent=ok?'Thank you. This frontend demo validated your enquiry locally.':'Please complete all required fields with valid information.';status.style.color=ok?'#d4b06f':'#ff8b8b';if(ok)form.reset()});
const lightbox=document.querySelector('.lightbox'),lightboxText=lightbox.querySelector('p');document.querySelectorAll('[data-lightbox]').forEach(btn=>btn.addEventListener('click',()=>{lightbox.classList.add('active');lightboxText.textContent=btn.dataset.lightbox}));lightbox.querySelector('button').addEventListener('click',()=>lightbox.classList.remove('active'));lightbox.addEventListener('click',e=>{if(e.target===lightbox)lightbox.classList.remove('active')});addEventListener('keydown',e=>{if(e.key==='Escape')lightbox.classList.remove('active')});
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const target=document.querySelector(a.getAttribute('href'));if(target){e.preventDefault();target.scrollIntoView({behavior:'smooth',block:'start'})}}));
